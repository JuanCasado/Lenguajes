package antlr;

import java.util.*;
import org.antlr.v4.runtime.tree.AbstractParseTreeVisitor;

class JsonVisitor extends JsonParserBaseVisitor<Integer> {
/*


    public JsonVisitor(JSONTable jsonT){
      
    }

    @Override
    public Integer visitInit(JsonParser.InitContext ctx) { 
       visit(ctx.graphs());
       return 0;
    }

    @Override public Integer visitGraphs(JsonParser.GraphsContext ctx){
        if (ctx.list()!=null){
            for (JsonParser.ListaContext numList: ctx.list()){
                visit(numList);
            }
        }
        return 0;
    }
 */   
}